const initialState = [
    {
        id:1,
        title: "Title One",
        description: "Description One"
    }
]

const tasks = (state= { tasks: initialState}, action) => {
    return state;
}

export default tasks;